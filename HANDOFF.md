# HANDOFF — Liquid Pixel Studio 完整交接文档

> 2026-08-22 写于项目 139 个 commit 之际(2026-07-21 首次迁移 → 今日)。
> 用途:换账号/换助手/若干年后重看时,一份文档看懂这个软件的全部 —— 它是什么、怎么建的、
> 踩过什么坑、下一个软件该怎么做。配合阅读:[CLAUDE.md](Claude.md)(项目宪法)、
> [EXPORT.md](EXPORT.md)(导出管线)、[PROGRESS.md](PROGRESS.md)(阶段快照)、git log(逐步实录)。

---

## 一、这是什么

**为自动驾驶汽车车尾 LED 点阵灯语(eHMI)造的专属创作工具** —— 设计师画形状/打字/导 SVG
→ 自动填充成点阵 → 多状态间平滑形变(metaball morph)→ 导出 MP4/PNG,经 LedArt/HDPlayer
下发到 HD-C16L 控制器驱动的 P2.5 LED 模组上屏。同时是 HRI 研究仪器(刺激材料生成器)。

**美学纲领(一切动效决策的最高准则):"光不闪烁,光生长。"**
状态间永远连续形变;缓动默认 smootherstep;停留期低频呼吸漂移;错峰制造波浪式生长;
任何 3–30Hz 亮度振荡都是光敏安全红线。

技术形态:**单 HTML 文件、vanilla JS、零框架、零服务器**;运行时依赖只有 JSZip + mp4-muxer。
`npm run dev` 开发,`npm run build` 出自包含的 `dist/index.html`(直接双击可用)。

---

## 二、建造时间线(一个月,五个阶段)

1. **迁移与采样器时代**(7/21–7/25):单文件 v4 原型拆成 Vite 模块;修 clumping
   (unbalanced OT 生死配对)、呼吸相位接缝;采样器家族成型(方格/六角/泊松/Lloyd 均匀/
   智能结构圆/笔画/灰阶点画/Vogel/同心环/仅轮廓)。
2. **矢量与 3D 时代**(7/25–8/1):钢笔贝塞尔、实心 SDF 渲染、AE 式链接图层矢量形变、
   木偶最短路径形变;three.js 车模预览器(投影面/环绕皮肤/UV 直贴/区域切割/Rhino 式 gumball)。
3. **品牌与物理时代**(8/1–8/8):改名 Liquid Pixel Studio,三语界面,HMG 设计语言;
   26 种实验物理场(情绪测试台);逐关键帧变频 chirp。
4. **角色与硬件时代**(8/8–8/15):并行角色轨(SVG 走路小人、地面速度模型、跑不停环绕);
   P1→P2 实装布局导出(逐模组裁切/旋转/平移、校准帧、反向变换、高清倍数)。
5. **修罗场时代**(8/15–8/22,本次长会话):导出时长/时间模型修复、界面-状态同步修复、
   镜像家族、探测帧方法论、逐模组翻转 —— 见第六节"血泪教训"。

---

## 三、架构(下一个软件照抄这个分层)

```
数据层(纯 JSON,可序列化)      state.js  states[]/shapes/characters/P 参数
采样层(纯函数)                samplers.js  (maskReader, params) => pts[]
引擎层(纯函数,核心不变量)     engine.js  sampleFrame(g) → {balls, solids, col, cam}
渲染层(CPU tile 场函数)       render.js  f(p)=Σr²/d²,阈值+柔度软边;render-gl.js GPU 快路径
导出层                         export.js  PNG zip / MP4(WebCodecs)/ WebM;共用 sampleFrame
硬件层(纯函数)                ledmap.js  模组映射表 + 变换;ledcanvas.js DOM 适配薄层
UI 层                          ui/*.js    面板互不 import 业务,经 store/P 通信
```

**三条铁律(它们救了这个项目无数次):**
1. **引擎纯函数**:任意时刻 g 可凭空求值 → 时间轴可拖、导出确定、预览==导出。
   一个月里所有 bug 都在边缘(设置/UI/时长),核心从未坏过 —— 因为它无状态。
2. **数据层与画布对象严格分离**:快照/存档只碰 JSON。
3. **工程文件向后兼容**:v3/v4 都能读;嵌套对象逐键并入(applyParams),数组整体替换。

---

## 四、功能全量清单(浓缩)

- **输入**:矩形/椭圆/文字/单点/钢笔贝塞尔;布尔加减;图片导入(阈值/半调/彩色 k-means);
  SVG 导入(含动画 SVG 按 keyTimes 采样、描边保留);图片序列导入。
- **排列(计算式)**:对齐/等距/等宽高/镜像复制/阵列/中线/持久约束(定距/等尺寸/对称/对中/正交)。
- **采样**:10 种算法;点数上限 1500 自动抽稀;逐形状采样与半径倍率。
- **形变引擎**:配对(最优传输/排序/角度/贪心/随机);缓动 10 种(含 spring/anticipate/hesitate);
  错峰/流场/拉伸/呼吸漂移;逐过渡参数覆盖;速度曲线(起步/落位贝塞尔);整体剪影变形;
  木偶逐锚点形变;实心⇄点阵溶解(融合判据)。
- **状态系统**:胶片条;停留/过渡时长;状态内子循环(眨眼/走路姿态);虚拟镜头(推拉摇移);
  动画片段(clip 整体调速/循环圈数)。
- **动效场**:停留期动态几何(波浪/弹簧/液态线/波纹/微光/边缘波/锚点);边缘几何(细波/锯齿/
  飞溅,几何级重建);26 种实验物理(重力/浮力/湍流/漩涡/结晶/愤怒尖刺/萤火虫/阳光/落叶…);
  墨水沉积(水墨,scope 可限单状态)。
- **角色(并行走动)**:帧组→角色;独立循环 + 步频× + 地面速度(与步频解耦)+ 走过用时反解;
  跑不停三向(↔横/↕竖/↻绕车带);镜像/延迟/跟主轴;深克隆副本。
- **3D 预览器**(viewer.html):GLB 车模;投影面/环绕皮肤/UV 直贴;区域切割;画笔/橡皮/上色;
  LED 拼版烘焙导出;与编辑器双向同步(localStorage 通道)。
- **导出**:MP4(WebCodecs H.264 逐帧确定)/PNG 序列/WebM;方形/自定义分辨率;2×超采样;辉光;
  ⏱指定导出时长(自动=主序列与角色圈的公倍数);📐宽画幅;🖥输出变换(画幅适配 4 模式含手动
  拉伸%、旋转/镜像/双边对称与铺满、N×M 网格 warp);🧩取景框(Blender);🔩P2 实装布局。
- **P2 硬件管线**:模组映射表(src/dst/rotate/flipX/flipY,界面可编辑);直通/旋转槽位双布局
  预设;正向/反向变换;逐模组旋转(槽位相容约束)与镜像;双边镜像三模式;双侧同显;
  导出倍数 1×–16×(零重采样);🎯校准帧(随工作流形状匹配);🧭探测帧(原样直发测真实接线)。
- **工程化**:保存/打开 .json;自动保存(15s,多标签页槽位);撤销 60 步;三语 i18n
  (zh 源文案 + EN/KO 字典 + MutationObserver 动态翻译);快捷键;中文注释。

---

## 五、硬件管线方法论(本月最贵的一课)

**硬件事实**:HD-C16L 控制器,128×320 帧缓冲,5 块 128×64 P2.5 模组;像素间距恒定 →
**永不缩放,只裁切/旋转/平移,最近邻零插值,送硬件必须 1×**。

**方法论(按此顺序,不要跳步):**
1. **先发 🧭 探测帧**(原样直发、绕过一切软件变换):每条带一色 + 大写 **F**(无旋转/镜像
   对称,朝向即真相)+ N 白点(=第几条带)。一张照片读出:哪条带到了哪块板、转了几度、有无镜像。
2. **按读数配置**:直通/旋转槽位布局 → 逐模组 0/90/180/270 → ⇋/⇅ 镜像 → 映射表 src/dst 调序。
3. **再发 🎯 校准帧验证**(它走与动画完全相同的变换路径 → 校准对 = 动画一定对)。
4. 之后才做动画。

**为什么这么规定**:我们曾用"看动画猜配置"的方式调了两周 —— 每个错误假设(横带接线?
旋转槽位?反向工作流?)都能解释一部分现象,催生一轮错误修复。**不对称探测图案 + 绕过变换
直发**是唯一能一次测出真相的手段。这方法适用于一切"软件坐标系 ↔ 物理设备"的调试。

---

## 六、工程血泪教训(下一个软件的护身符)

1. **界面必须永远如实反映生效状态。** 最痛的 bug:面板 init 读默认参数,自动保存**之后**
   才恢复存档 → 复选框显示"关"而内存是"开",导出按隐藏状态走("什么都没勾却导出 1500×600
   拉伸")。修法:恢复/打开工程后统一 syncUIFromState。**教训:凡有"加载存档"的软件,
   必须有一条"状态→UI 全量回写"的通道,且在每次加载后调用。**
2. **时间模型要有单一真相。** states/过渡/子循环/clip/角色墙钟 = 五套并行计时系统,靠
   sync/delay/取模互相桥接 —— 本月一半的 bug(导出超长冻结、角色不同步、时长错乱)源于此。
   下一个软件:**一条时间轴,一切皆轨道,每个属性都是可关键帧的通道**(AE 模型)。
3. **诊断工具必须与生产路径同构。** 校准帧曾走"永远正向"的特殊路径,而动画走用户当前
   方向 → "校准看着对了,动画却错"。修成模式匹配后,"校准对"才真正蕴含"动画对"。
4. **全局可变配置是 bug 之源。** 一个共享 P 对象被所有面板读写 + 自动保存持久化 = 设置
   在多轮调试中悄悄漂移。下一个软件:**具名不可变预设**(AE Render Queue 模型),导出时
   选择预设而不是累积环境状态。
5. **纯函数核心 = 可测试、可并行、可信。** node 断言 134 条 + 浏览器像素级验证,每次修复
   都能数学证明。golden-frame(参考帧像素哈希)值得从第一天就上 CI。
6. **动手前先备份**(zip + git tag),重构前先 commit;每个功能一个小 commit,
   conventional message —— 本项目 139 个 commit 就是全部建造史,本文档一半内容由它重建。
7. **削减先于重构。** 面对"想重写"的冲动:正确答案几乎总是"删掉不用的 + 绞杀式逐器官
   替换",而不是从零重建 —— 重写会丢掉全部血泪知识(P2 怪癖、兼容性、边缘 case)。
8. **坐标系约定要写死并可视化。** 归一化 vs 像素、屏幕 y 向下、CW vs CCW、全角/半角标点
   进 i18n 字典 —— 每一个都咬过人。凡跨坐标系,写注释 + 画探测图。

---

## 七、日常工作流(从画到上屏)

1. 画/导入形状 → 采样成点(或实心) → 建状态序列(停留/过渡) → 调缓动/错峰/效果
2. 角色:选中帧→角色,设走位(跑不停/地面速度/走过用时/延迟错峰)
3. 预览 == 导出,放心拖时间轴;3D 预览器看车身效果
4. 屏幕成片:🖥输出变换(2880×1200 等比留边)→ MP4
5. 上灯:🔩P2(先探测帧→校准帧流程,见第五节)→ 1× MP4/PNG → HDPlayer/LedArt 下发
6. `ffmpeg -framerate 30 -i frame_%04d.png -pix_fmt yuv420p out.mp4`(PNG 合成参考)

---

## 八、下一代软件的设计蓝图(若从这里出发)

**近路线(在本代码库上)**:①功能审计削减 → ②具名导出预设 → ③统一时间轴(Track 抽象,
绞杀式迁移,引擎不动)→ ④WebGL/WebGPU 导出渲染(CPU 作 golden 基准)→ ⑤可视化输出
patch 编辑器(拖拽模组 + 点击闪灯,替代数字表)。

**研究仪器方向(独特身份)**:sidecar JSON(每次导出附完整参数向量 + git hash + 事件时间轴
+ 客观度量:亮度统计/主频/光敏判定)→ 刺激 ID 可引用可复现;参数扫掠批量导出(析因设计
自动生成);演出模式记录呈现日志 → 与被试反应按 ID 连接,做混合效应模型
(rating ~ stagger × easing + (1|participant))—— 从"比较片段"升级为"建模媒介心理物理"。

**不要抄大软件的**:节点图、插件生态、docking UI。**要保住的**:单文件零安装、本地化、
确定性、中文注释、"光生长"纲领。

---

## 九、关键公式与常数速查

- lerp `a+(b−a)t`;smootherstep `6t⁵−15t⁴+10t³`;场函数 `f(p)=Σr²/|p−c|²`,f≥thr 实心
- 半调 `r = R_max·√B`;**融合判据** `r ≥ (s/2)·√(thr/2)`(实体⇄点云临界线)
- Vogel 螺旋 `r=c·√n, θ=n·137.508°`;泊松盘 cell=minD/√2 网格加速
- gamma:LED 建议 γ≈2.2 起步;90°CW 旋转 `dx=H−1−sy, dy=sx`(屏幕坐标,y 向下)
- 画布 480²;SDF 3×;点上限 1500;性能红线 480×280+1500 点 ≥30fps;导出帧上限 1200
- 角色:跑不停一圈 ≈ (画布边+200)/地面速度;走路循环整数圈对齐 → 首尾无缝

---

## 十、资源指针

- 仓库:https://github.com/WendyTang31/Liquid-Pixel-studio (git log = 逐日建造实录)
- 本机历史会话(CCD):Metaball morph studio prototype (7/27) → build (7/29) →
  emotion-driven system (8/5) → P1 edge resolution (8/15) → 本次导出/硬件长会话 (8/15–22)
- 回滚点:tag `backup-pre-export-fix`;桌面 `Metaball_Morph_Studio_BACKUP_2026-08-15.zip`
- 硬件规格原始 spec:见 8/22 会话中的 "ALIAS — Animation Tool Handoff" 文档(P1/P2 布局表、
  90°CW 验证、验收测试五条)
