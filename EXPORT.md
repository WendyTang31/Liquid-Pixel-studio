# 导出逻辑说明(EXPORT.md)

> 2026-08-15 整理。备份:git tag `backup-pre-export-fix` + 桌面 `Metaball_Morph_Studio_BACKUP_2026-08-15.zip`。

## 一、导出管线(主流视频软件的对应关系)

类比 AE/Premiere:**合成(Comp)→ 输出模块(Output Module)**。

```
① 基础渲染(方形合成,固定 480² 逻辑坐标,任意分辨率逐帧确定性渲染)
      sampleFrame(gm) + computeVectorPolys(gm) + 角色 charSolids(g)
② 输出变换(可选,🖥 输出变换 = 输出模块):画幅适配 + 镜像/旋转/warp + 双边镜像
③ 编码:MP4(WebCodecs H.264)/ PNG 序列(zip)/ WebM(实时录制)
```

互斥的四种「输出模式」(exclusiveExportMode 保证只开一个):
- **普通方形**:什么都不勾 → 按「⑤ 导出」的宽高直接渲。
- **🖥 输出变换**:方形动画 → 任意画幅(36:15 等)。
- **🔩 P2 实装布局**:LED 硬件专用(128×320,勿动,已可用)。
- **📐 宽画幅 / 🧩 取景框**:特殊用途。

## 二、时间模型(本次修复的核心)

- **主时间轴**(states/frames):周期 T = 各段停留+过渡之和。导出时间 g 超过 T 时
  **取模循环 gm = g % T**(和预览播放一样)。
  ⚠ 修复前:sampleFrame 把 g 钳在 T 末端 → 导出比 T 长时,主动画冻结/变形/停留帧消失。
- **角色(并行走动)**:独立墙钟 g(不取模)→ 跑不停/横穿可以比 T 长得多。
- **导出总时长**:
  - 自动:主序列与所有墙钟角色圈的公倍数(全部整圈 → 首尾无缝);
  - 手动:「⏱ 指定导出时长」直接给秒数(注意:不是 T 的整数倍时,视频结尾主动画会切在半途,
    循环播放时主动画有接缝;角色无缝)。

## 三、画幅适配(输出变换的「适配」下拉)

| 模式 | 变形? | 行为 |
|---|---|---|
| 等比留边 fit | ✅ 无变形 | 图形原比例,空余处填**画布背景色**(白底→填白) |
| 等比裁满 fill | ✅ 无变形 | 放大盖满画幅,超出部分裁掉 |
| 拉伸填满 stretch | ❌ 会变形 | 强行铺满(仅 LED 长条等特殊用途) |
| 手动拉伸 manual | 自定 | 横/纵 % 各自定倍率(100=不变形),空余填背景色 |

**要"无变形 + 填满 36:15 白底" → 用「等比留边」。**

## 四、你的 15s · 36:15 · 无变形 MP4 菜谱

1. ⑤ 导出:基础分辨率 2048×2048(质量上限;输出变换开着时会自动只渲输出需要的大小,不浪费)。
2. 勾 **🖥 输出变换**:画幅 **2880×1200**(36:15,比 1440×600 像素高一倍),适配 **等比留边**。
3. 时长:**不勾** ⏱(自动整圈无缝);或勾上填 15。读数区显示「实际导出 ≈ Xs · N 帧」。
4. 帧率 24–30;**着急就把「2×超采样」关掉**(约快 4 倍,SDF 软边本身有抗锯齿)。
5. 🎬 MP4。

## 五、性能要点

- 输出变换开启时,基础方形自动缩到输出真正需要的分辨率(等比留边 2880×1200 → 只渲 1200²;
  修复前渲 2048²+2×超采样 = 4096²,每帧 1600 万像素,这是"导出慢"的主因)。
- renderToImageData 的 ImageData/tile bins 按画布复用,不再每帧重新分配(2048² 每帧省 16MB 分配)。
- 实测参考(此机器):2880×1200 + 2×超采样 ≈ 2s/帧;关超采样 ≈ 0.5s/帧。
  15s@30fps=450 帧 → 15 分钟 / 4 分钟。降 fps 线性加速。

## 六、角色 ⇄ 帧(现状约定,后续再优化)

- **帧(states)** = 主时间轴,顺序播放,T 固定。
- **角色** = 并行轨:一段帧序列打包 + 独立循环(步频×/时长)+ 走位(左右/上下/地面速度/跑不停)。
- 帧→角色:胶片条 Shift 多选 → 「＋ 选中帧→角色」(帧被移出主时间轴)。
- 角色→帧:进入角色编辑(点卡片)后其帧就在胶片条上,可 Ctrl+X/V 搬回。
- 同步:角色卡「⏱自由 / 🔗跟主轴」+ 延迟;跑不停强制走墙钟(与跟主轴互斥,防回跳)。
- **导出一致性**:预览与导出对同一帧号像素级一致(全部纯函数,g 凭空求值)。
- ⚠ 正在「✏ 编辑中」的角色不进导出(它占着主时间轴)—— 导出前先「✓ 完成编辑角色」。
